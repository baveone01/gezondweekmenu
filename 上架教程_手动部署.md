# gezondweekmenu.snapvil.com 手动上架教程（Cloudflare Pages 版）

> 项目已推送：https://github.com/baveone01/gezondweekmenu
> 类型：纯静态网站（Astro 4，构建产物输出到 `dist/`）
> 部署方式：沿用你的统一模式 —— **Cloudflare Pages 免费托管 + snapvil.com 子域名 CNAME**
> 本文已把站点所有 canonical / sitemap / 结构化数据 / 社媒分享 URL 统一改成正式子域 `gezondweekmenu.snapvil.com`（提交 `4fe7e35` 已推送）

---

## 一、你需要知道的（一次讲清）

- **不需要购买新域名**。主域 `snapvil.com` 已在你的 DNS 注册商处管理（就是你截图那个"高级 DNS"界面）。
- **自定义域名 = `gezondweekmenu.snapvil.com`**，也就是本项目的正式域名。
- 部署分两半，都要做：
  1. **Cloudflare Pages** 里创建项目并接入"自定义域名"
  2. **你的 DNS 注册商**里加一条 CNAME 记录指过去

---

## 二、第一步：Cloudflare Pages 创建项目并部署

1. 登录 Cloudflare Dashboard：https://dash.cloudflare.com
2. 左侧菜单 **Workers & Pages → Create → Pages → Connect to Git**。
3. 点击 **Connect GitHub**，授权后选中仓库 `baveone01/gezondweekmenu`。
4. 生产分支选 `main`。
5. **Framework preset 选 `Astro`**；
   - Build command：`npm run build`
   - Output directory：`dist`
6. 点 **Save and Deploy**，约 1–2 分钟完成，得到预览地址（形如 `xxxx.gezondweekmenu.pages.dev`）。

> 项目名 Cloudflare 会自动取 `gezondweekmenu`，默认预览域就是 `gezondweekmenu.pages.dev`（这是下面 DNS CNAME 的目标）。

---

## 三、第二步：绑定自定义域名 `gezondweekmenu.snapvil.com`

### A. 在 Cloudflare Pages 里添加自定义域名
1. 打开该项目 → **Settings（设置）→ Custom domains（自定义域名）→ Set up a custom domain**。
2. 输入 `gezondweekmenu.snapvil.com`，它会提示你按下面方式在 DNS 注册商添加记录。

### B. 在你的 DNS 注册商（"高级 DNS"界面）加一条 CNAME 记录
照你已有的那排记录格式添加这条（和你截图里的 hscode / mel / sea 等完全同款）：

| Host（主机/名称） | Type（类型） | Value（目标/值） | TTL |
|---|---|---|---|
| `gezondweekmenu` | `CNAME` | `gezondweekmenu.pages.dev` | 30 分钟 |

- Host 填 `gezondweekmenu`（不要填 `@`，也不要带 `.snapvil.com`）。
- 结果：`gezondweekmenu.snapvil.com` → CNAME → `gezondweekmenu.pages.dev`。
- TTL 选 **30 分钟**（1800），和你现有记录一致。

> 注意链接：Cloudflare Pages 预览域是 `gezondweekmenu.pages.dev`（`<项目名>.pages.dev`）。DNS 值填这个，别填 Cloudflare 给你的 `xxxx.pages.dev`。

### C. 等待生效
- DNS 传播约几分钟到 30 分钟（TTL 30 分钟）。
- Cloudflare 会在解析成功后自动签发 **HTTPS 证书**（几个小时内自动完成，无需手动）。

---

## 四、第三种：可选（非必需）——用 `www` 子域
如果你想同时支持 `www.gezondweekmenu.snapvil.com`，可再加一条同款 CNAME（Host 填 `www.gezondweekmenu`，值同 `gezondweekmenu.pages.dev`）。非必需，建议先跑通主域。

---

## 五、上线验收清单（必查）

1. **页面可访问**：打开 `https://gezondweekmenu.snapvil.com`，确认主页、`/recepten/`、`/weekmenu/`、`/blog/`、`/boodschappenlijst/` 均正常。
2. **HTTPS 正常**：地址栏有锁、证书有效。
3. **canonical 正确**：任意菜谱页源码里 `<link rel="canonical">` 指向 `https://gezondweekmenu.snapvil.com/...`（已改好，无需再动）。
4. **robots + sitemap**：
   - `https://gezondweekmenu.snapvil.com/robots.txt`
   - `https://gezondweekmenu.snapvil.com/sitemap-index.xml`
5. **结构化数据**：用 Google 富媒体测试确认 Recipe / FAQ 正常：
   - https://search.google.com/test/rich-results （输入菜谱/博客 URL）
6. **Google Search Console 提交**（流量关键，必做）：
   - https://search.google.com/search-console
   - 添加 `snapvil.com` 并验证，找到 `gezondweekmenu` 子域名；或添加 `https://gezondweekmenu.snapvil.com/` 属性
   - 在「站点地图」提交 `https://gezondweekmenu.snapvil.com/sitemap-index.xml`
7. **法律页齐全**：`/privacybeleid/`、`/cookiebeleid/`、`/disclaimer/`、`/over-ons/`、`/contact/` 均已内置。

---

## 六、日常更新如何上线

改内容（加食谱、周菜单或博客）后：

```bash
npm run build
git add .
git commit -m "描述这次改动"
git push
```

推送到 GitHub `main` 后，Cloudflare Pages 会自动重新构建并发布 —— 不需要手动传文件。

> 若本地构建通过但线上失败，多半是平台 Node 版本过旧，在 Cloudflare Pages 的 **Settings → Builds & deployments → Node version** 设为 `20`。

---

## 七、流量与变现（上线后建议）

已内置流量抓手：
- **16 个食谱 + 5 个周菜单** → Recipe 富摘要、长尾关键词
- **博客（信息型内容）** → FAQ / Article schema，吸引 Google 问答流量并内链到菜谱页
- **购物清单工具** → 增强停留时长与回访

变现建议：接入荷兰本地电商联盟（Bol.com Partnerprogramma、Amazon Associates NL 等），与食谱/厨房用品强相关，点击与转化率更高。