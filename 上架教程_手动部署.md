# gezondweekmenu.nl 手动上架教程

> 项目已推送：https://github.com/baveone01/gezondweekmenu
> 类型：**纯静态网站**（Astro 4，构建产物输出到 `dist/`）
> 用官方免费 + 静态托管方案即可上线，无需服务器。

---

## 一、上线前准备

### 1. 拥有域名（必做）
你需要在某注册商（如 Namecheap、GoDaddy、Netinhist、hostnet 等）注册 **gezondweekmenu.nl** 域名。
- `.nl` 为荷兰域名，注册需按注册商要求填写荷兰联络人信息（可填自己，中国地址也能注册，部分注册商需荷兰联系信息）。
- 域名费用约 **€3–10/年**（Namecheap/注册商差异较大，可多家比价）。

### 2. 本机验证构建（可选，确认无误）
在项目目录执行：

```bash
npm install
npm run build
```

构建成功后会生成 `dist/` 文件夹（约 30+ 个页面），用 `npm run preview` 本地预览效果。

---

## 二、选择托管平台（三选一，均为免费层）

| 平台 | 优点 | 适用 |
|------|------|------|
| **Cloudflare Pages** ★推荐 | 免费、全球 CDN 快、附送免费 SSL、荷兰访问快、可接 .nl | 荷兰语站首选 |
| **Netlify** | 配套简单、自带表单服务、步骤直观 | 新手友好 |
| **Vercel** | 前端生态好、预览部署方便 | 开发者常用 |

以下以最省事的三种分别给出步骤，**任选其一即可**。推荐 **Cloudflare Pages**（对 .nl 站点访问速度最好）。

---

## 三、方案 A：Cloudflare Pages（推荐）

1. 注册/登录 Cloudflare：https://dash.cloudflare.com/sign-up
2. 左侧菜单选 **Workers & Pages → Create → Pages → Connect to Git**。
3. 点击 **Connect GitHub**，授权后选中 `baveone01/gezondweekmenu` 仓库。
4. 生产分支选 `main`。
5. **Framework preset 选 `Astro`**；构建命令 `npm run build`；输出目录 `dist`。
6. 点击 **Save and Deploy**，等 1–2 分钟即可获得 `xxx.pages.dev` 预览地址。
7. 添加自定义域名：**Custom domains → Set up a custom domain**，填 `gezondweekmenu.nl`，按提示在域名注册商添加 CNAME 记录：
   - `CNAME` 指向 `xxx.pages.dev`
8. 等项目签发 SSL 证书（几分钟），正式上线。

---

## 四、方案 B：Netlify

1. 注册：https://app.netlify.com/signup
2. 登录后点 **Add new site → Import an existing project → GitHub**。
3. 授权并选择 `gezondweekmenu` 仓库。
4. 站点配置：
   - **Build command**：`npm run build`
   - **Publish directory**：`dist`
5. 点 **Deploy site**，得到 `gezondweekmenu.netlify.app`。
6. 域名：**Domain settings → Add custom domain**，填 `gezondweekmenu.nl`。
7. 到域名注册商处把 DNS 改为 Netlify 提供的 `A`（`75.2.60.5`）或 `CNAME`，等生效。

---

## 五、方案 C：Vercel

1. 注册：https://vercel.com/signup
2. **Add New → Project**，导入 `baveone01/gezondweekmenu`。
3. Vercel 会自动识别 Astro；确认 **Build Command = `npm run build`，Output = `dist`**（不用改）。
4. **Deploy**，得到 `gezondweekmenu.vercel.app`。
5. 域名：**Project → Settings → Domains**，填 `gezondweekmenu.nl`，并到注册商加 DNS 记录（TXT 验证 + `CNAME` 指向 `cname.vercel-dns.com`）。

---

## 六、线上验收清单（上线后必查）

1. **页面可访问**：打开 `https://gezondweekmenu.nl`，主页、`/recepten/`、`/weekmenu/`、`/blog/`、`/boodschappenlijst/` 均正常。
2. **HTTPS 正常**：地址栏带锁，证书有效。
3. **robots 与 sitemap**：访问 `https://gezondweekmenu.nl/robots.txt` 和 `https://gezondweekmenu.nl/sitemap-index.xml` 能打开。
4. **结构化数据**：用 Google 富媒体测试工具确认 Recipe/FAQ 正常：
   - https://search.google.com/test/rich-results
5. **Google Search Console 提交**（流量关键，必做）：
   - https://search.google.com/search-console
   - 添加 `gezondweekmenu.nl` 属性（用域名或网域验证），在「站点地图」提交 `https://gezondweekmenu.nl/sitemap-index.xml`。
6. 保留页面：`/privacybeleid/`、`/cookiebeleid/`、`/disclaimer/`、`/over-ons/`、`/contact/` 均已内置，符合底线合规。

---

## 七、日常更新如何上线

改内容（比如加食谱或博客）后：

```bash
npm run build
git add .
git commit -m "描述这次改动"
git push
```

推送到 GitHub `main` 分支后，Cloudflare/Netlify/Vercel 会自动重新构建并发布 —— **不用每次手动传文件**。

> 小提示：托管平台自动构建时会用 `npm install` + `npm run build`，与本地一致；若本地构建通过、线上失败，多为平台 Node 版本过旧，可在平台 Settings 里把 Node.js 设为 **18 或 20**。

---

## 八、流量与变现（上线后建议）

已内置的流量抓手：
- **16 个食谱 + 5 个周菜单** → Recipe 富摘要、长尾词
- **博客（信息型内容）** → FAQ/Article schema，吸引谷歌问答流量后内链到菜谱页
- **购物清单工具** → 增强停留与回访

后续变现建议：接入**荷兰食品/厨房用品联盟**（如 Bol.com Partnerprogramma、Amazon Associates NL、CJ NL 类目）与菜品直接相关，点击率、转化率更高。